{
  "name": "idumb-plugin-v2",
  "version": "2.0.0-clean.3",
  "description": "Intelligent Delegation Using Managed Boundaries — governance substrate for agentic CLIs",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "test": "npx tsx tests/tool-gate.test.ts && npx tsx tests/compaction.test.ts"
  },
  "dependencies": {
    "@opencode-ai/plugin": "^1.1.52"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=18"
  }
}
